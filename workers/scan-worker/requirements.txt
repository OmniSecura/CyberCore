celery[redis]>=5.4.0
redis>=5.0.0
SQLAlchemy~=2.0.41
psycopg[binary]>=3.1
bandit>=1.8.0
semgrep>=1.80.0
gitpython>=3.1.43
pip-audit>=2.7.0
# DAST: lightweight HTTP client for talking to the ZAP daemon's REST API.
# Intentionally not pulling python-owasp-zap-v2.4 — it's a thin wrapper over
# the same REST endpoints and brings extra transitive deps we don't need.
httpx>=0.27.0
